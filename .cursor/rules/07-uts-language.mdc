---
globs: *.uts,*.uvue
description: UTS language rules for types, functions, classes, and UTSJSONObject best practices
---

## UTS 语言规则与最佳实践

- **语言定位与编译目标**: UTS 是强类型跨端语言，编译为 Web/小程序→JavaScript，Android→Kotlin，iOS→Swift，HarmonyOS→ArkTS。语法贴近 TypeScript，但为跨端有约束与增补。
- **类型与变量**
  - 使用 `let`/`const` 声明并显式标注类型（尤其对导出 API）。
  - 支持联合类型与类型推导；对可为 null 的类型使用安全访问 `?.`。
  - 多行末尾分号可省略；同行多语句需使用分号分隔。
- **模块与导出**
  - 使用命名导出：`export function` / `export class` / `export type`。
  - 插件场景避免 `export default`；统一的入口签名利于摇树优化。
- **函数（function）**
  - 推荐函数声明式导出：`export function foo(arg: T): R {}`。导出的返回值请显式标注类型。
  - 支持默认参数、可选参数与剩余参数；在 uni-app Android 环境避免函数重载，用不同函数名区分。
  - 回调参数需强类型。若需持续触发回调且函数名不以 `on` 且仅含一个回调参数，则需在实现端使用 `@UTSJS.keepAlive`（两端保持一致）。
  - 箭头函数可用，但装饰器当前不支持 `export const fn = () => {}` 的导出方式。
- **类（class）**
  - 使用 `export class` 定义。字段与构造器参数显式标注类型；方法返回值显式标注。
  - 支持实例方法与静态方法；需要持续回调的实例/静态方法在实现端可使用 `@UTSJS.keepAlive`。
  - 避免方法重载（在 uni-app Android 环境可能无法区分）。
- **内置对象：UTSJSONObject**
  - 用于表示动态 JSON 对象的跨端容器；常见于网络响应或与平台 API 交互。
  - 可通过断言或转换获取：例如 `const obj = data as UTSJSONObject`；类型判断可用 `obj instanceof UTSJSONObject`。
  - 访问属性后请进行类型断言（或窄化）：`const name = obj["name"] as string`。建议尽量定义强类型模型，减少在业务层广泛使用 `UTSJSONObject`。
- **数组与类型判断**
  - 判断数组使用 `Array.isArray(val)` 或 `val instanceof Array`。
  - `instanceof` 可对内置对象（如 `Date`、`UTSJSONObject`）做布尔判断。
- **条件编译与平台差异**
  - 通过条件编译适配平台特性；平台专属 API 需隔离在条件分支内，避免破坏跨端兼容。

权威参考：
- UTS 语言总览与差异: https://doc.dcloud.net.cn/uni-app-x/uts/
- 函数（function）: https://doc.dcloud.net.cn/uni-app-x/uts/function.html
- 类（class）: https://doc.dcloud.net.cn/uni-app-x/uts/class.html
- 内置对象 UTSJSONObject: https://doc.dcloud.net.cn/uni-app-x/uts/buildin-object-api/utsjsonobject.html

