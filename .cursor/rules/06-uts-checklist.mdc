---
globs: *.uts
alwaysApply: false
---
## UTS 插件开发 Checklist（提交前自查）

- [ ] `interface.uts`：方法/类型声明完整，跨端一致；导出为命名导出。
- [ ] `unierror.uts`：错误域与错误码齐全；`createError` 等工具可用。
- [ ] 端实现：`app-android/index.uts`、`app-ios/index.uts` 对齐 `interface.uts`。
- [ ] 仅 import 插件内部文件，未引用插件目录外部。
- [ ] 回调策略：
  - [ ] 事件订阅 API 使用 `onXxx/offXxx`；`off` 幂等。
  - [ ] 持续回调使用 `@UTSJS.keepAlive` 或 `on` 规则；两端均配置。
  - [ ] 提供 `stop/cancel` 或 `unsubscribe` 释放路径。
- [ ] Android：
  - [ ] 未使用重载；或已用不同函数名区分。
  - [ ] `config.json` 权限/网络/混淆声明完整；AAR 放于 `libs/` 并声明。
- [ ] iOS：
  - [ ] `config.json` 注入 `Info.plist` 权限；Frameworks 声明与签名正确。
  - [ ] 模拟器/真机架构齐备（必要时）。
- [ ] API 形态：
  - [ ] 单次任务 Promise 化；支持 `success/fail/complete`（可选）。
  - [ ] 组件/界面 API 不嵌入 template（除组件插件）。
- [ ] 生命周期：进入后台/页面卸载时释放原生资源与回调。
- [ ] 文档与示例：README 用法、参数、返回、错误、平台差异说明清晰。

参考：
- [UTS 插件（官方）](https://doc.dcloud.net.cn/uni-app-x/plugin/uts-plugin.html)
- [UTS 语言与平台说明（官方）](https://doc.dcloud.net.cn/uni-app-x/uts/)

